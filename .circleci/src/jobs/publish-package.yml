executor: python
steps:
  - checkout
  - attach_workspace:
      at: .
  - activate-venv
  - run:
      name: Check via twine
      command: twine check ./dist/lunes*.tar.gz
  - run:
      name: Publish lunes-cms package to (Test-)PyPI
      command: |
        ls -lh ./dist/*
        twine upload --non-interactive --verbose ./dist/lunes*.tar.gz
  - notify
