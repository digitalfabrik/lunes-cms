executor: python
steps:
  - checkout
  - attach_workspace:
      at: .
  - activate-venv
  - run:
      name: Install pylint requirements
      command: sudo apt-get update && sudo apt-get install ffmpeg
  - run:
      name: Run pylint
      command: |
        pip install pylint
        ./tools/pylint.sh
  - notify
